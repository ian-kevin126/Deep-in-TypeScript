console.log('******** ============ start ============ **********');

function selfMultiply(one: string | number) {
  return (one as number) + 3;
}

console.log('******** ============ end ============ **********');
export {};
